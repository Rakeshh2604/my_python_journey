# in python we can change the data type of a variable called as casting. we can change the datatype of a float to int where it removes the decimal and converts it into an integer and can even be done to string datatype too.
x=10
y=float(x)
print(y)
z=str(11)
print(z)
print(type(z))
